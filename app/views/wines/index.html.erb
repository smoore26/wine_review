<h1>Wines</h1>
<p> Available as of <%= formatted_time(@available_at) %></p>
<ol>
	<% @wines.each do |wine| %>
	<li><%= link_to(wine.name, wine) %> a <%= wine.varietal %> from <%= wine.winery %></li>
	<% end %>
</ol>

<%= paginate @wines %>

<%= link_to 'New Wine', new_wine_path %>
 
